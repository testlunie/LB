name: blank

on:
  push:

jobs:
  inherit:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        divide: ["previous", "leap", "crowd", "instruct", "survey"]
        exaggerate: ["odd", "eliminate", "clue", "fluid"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar CX
